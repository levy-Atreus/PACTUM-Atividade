name: CI e Publicação de Resultados (Node.js)

on:
  push:
    branches: [ "main", "exercicios/modulo-4" ] 
  workflow_dispatch:

jobs:
  test_and_publish:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write 
      pages: write
      id-token: write

    steps:
    - name: ⬇️ Checkout do Código
      uses: actions/checkout@v4

    - name: ⚙️ Configurar Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20' 
        
    - name: 📦 Instalar Dependências
      run: npm install

    # AJUSTADO: Agora focado em API Externa e Contratos Mockados
    - name: 🧪 Rodar Testes (API EBAC + Contratos)
      run: npm run test:ci